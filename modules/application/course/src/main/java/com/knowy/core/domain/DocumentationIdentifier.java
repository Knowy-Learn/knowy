package com.knowy.core.domain;

/**
 * Identity Contract for Documentation.
 * <p>
 * Used when referencing documentation by its unique key without requiring the full content body.
 */
public interface DocumentationIdentifier {
	int id();
}
